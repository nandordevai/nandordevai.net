<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nándor Dévai – CV</title>
    <link rel="stylesheet" href="/common.css">
    <style>
        :root {
            --content-width: 50rem;
        }

        a, a:visited, a:active {
            color: black;
        }

        body {
            font-family: 'Helvetica Neue', 'Helvetica', 'Arial', sans-serif;
            font-weight: 300;
            margin-block: 5rem;

            @media screen and (max-width: 768px) {
                margin: 1rem;
            }
        }

        footer {
            margin: auto;
            margin-block-start: 2rem;
            width: var(--content-width);
        }

        h1, h2 {
            font-weight: 400;
            line-height: 1;

            &::first-line {
                font-weight: 200;
            }
        }

        h1 {
            font-size: 2.5rem;
        }

        h2 {
            border-block-end: 0.5px solid black;
            font-size: 1.5rem;
            left: -100rem;
            padding-inline-start: 100rem;
            position: relative;
            width: 15rem;
        }

        h3 {
            font-size: 1rem;
            font-weight: 400;
            margin-block-end: 0;
        }

        main {
            margin: auto;
            width: var(--content-width);
        }

        p, ul {
            line-height: 1.75;
            margin-block: 0;
        }

        ul {
            list-style-type: none;
            padding: 0;

            li::before {
                content: '_';
                margin-inline-start: -0.5rem;
            }
        }
    </style>
</head>
<body>
    <main>
        {{ content | safe }}
    </main>
    <footer>
        <a class="button" href="/Nandor Devai CV.pdf">Download as PDF</a>
    </footer>
</body>
</html>
